image:
  name: docker/compose
  entrypoint: ["/bin/sh", "-c"]

services:
  - docker:dind

before_script:
  - docker-compose --version
  - docker info

build:
  stage: build
  script:
    - docker-compose up -d

test:
  stage: test
  script:
    - docker-compose exec client npm run test
