version: '3.7'
services:
  client:
    build:
      context: ./app/
    command: npm start
    volumes:
      - ./app/public:/app/public
      - ./app/src:/app/src
      - /app/node_modules
    ports:
      - "3000:3000"